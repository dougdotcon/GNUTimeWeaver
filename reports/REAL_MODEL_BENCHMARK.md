# Real model benchmark

Benchmark schema covers replay versus restore at supported prefix lengths (128/512/1024/2048), token counts, serialization/restore timings, bytes, state size, RSS, and cold/warm runs. No measurements exist until `TIMEWEAVER_MODEL_PATH` and a pinned llama.cpp tree are supplied.
