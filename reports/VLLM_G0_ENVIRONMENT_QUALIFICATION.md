# G0 environment qualification

Result: `G0_ENVIRONMENT_NOT_READY`. Native Windows; no WSL distribution; host
Python 3.14.6; PyTorch and vLLM absent. The official tag and API source are
fingerprinted, but no Linux Python 3.12 runtime, CPU inference, external
connector load, or model exists.
