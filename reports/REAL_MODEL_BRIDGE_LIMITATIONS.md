# Limitations

Status is `ADAPTER_PROTOCOL_READY_NO_REAL_MODEL`. The bridge does not provide CUDA, vLLM, block-native KV, VRAM CoW, zero-copy restore, hot swapping, arbitrary prefix mutation, distributed resume, or arbitrary kernel pause.
