# Block identity availability

Runtime/config/model fields are classified only after real v0.23.0 events.
Missing identity-critical fields will mark `identity_incomplete`; no complete
persistent key is asserted from mock data.
