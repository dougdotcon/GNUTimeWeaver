# Acceptance no-prefix redecode

For all three acceptance lengths,
`prefix_tokens_decoded_after_restore=0`. Only the separately saved pending
generated token and new suffix batches were decoded after restore.
