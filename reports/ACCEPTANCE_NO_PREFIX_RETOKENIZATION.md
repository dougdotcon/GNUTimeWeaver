# Acceptance no-prefix retokenization

For 512, 1024 and 2048 token prefixes,
`prefix_tokenizer_invocations_after_restore=0` and
`prefix_tokens_tokenized_after_restore=0`. New suffix tokenization is tracked
separately.
