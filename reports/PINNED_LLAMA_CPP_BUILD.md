# Pinned llama.cpp build

Required: `TIMEWEAVER_LLAMA_CPP_DIR`, clean tree, `git rev-parse HEAD ==
c588c4f`, release b10103. CMake integration is opt-in with
`-DTIMEWEAVER_WITH_LLAMA_CPP=ON`; default MVP build remains OFF.
