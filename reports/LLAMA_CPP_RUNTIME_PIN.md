# llama.cpp runtime pin

Target: `ggml-org/llama.cpp`; integration: externally provided source tree. No checkout was present at baseline, so `runtime_commit`, API version, compiler flags, and CPU feature set remain **unresolved**. Execution must refuse to claim verification until these fields are filled from the supplied tree.
