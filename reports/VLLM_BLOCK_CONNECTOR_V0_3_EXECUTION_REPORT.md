# v0.3 execution report

Status: `VLLM_CONNECTOR_PROTOCOL_READY_NO_SUPPORTED_ENVIRONMENT`.

G0 is blocked: this host is Windows without a WSL distribution; Python 3.14.6
has neither PyTorch nor vLLM. No vLLM event, block payload, scheduler restore,
GPU or cross-engine result is claimed. The external connector package,
identity, event mirror and immutable store contracts are prepared.
