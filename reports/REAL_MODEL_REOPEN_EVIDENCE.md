# Reopen evidence

The required test closes the process and workspace, starts a new process, validates object hashes, reloads the model, restores sequence state, and continues. Not executed because no pinned runtime/model is available.
