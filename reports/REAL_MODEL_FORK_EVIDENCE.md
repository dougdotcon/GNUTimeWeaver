# Fork evidence

Planned proof: restore one immutable parent checkpoint, append `suffix_A` and `suffix_B` in separate sequences, then restore the parent again. Each branch records its parent node/object hash. No real run is recorded in this environment.
