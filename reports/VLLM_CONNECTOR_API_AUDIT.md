# Connector API audit

`KVConnectorBase_V1` is not importable on this host. The package entry point is
`python/timeweaver_vllm/connector.py`; exact method signatures remain pending
the pinned vLLM tree and are intentionally not fabricated.
