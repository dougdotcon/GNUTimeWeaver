# GGUF fingerprints

The pinned runtime is available, but no local model was available at
`TIMEWEAVER_MODEL_PATH`. Fingerprints are intentionally empty and must be
calculated locally (SHA-256, size, architecture, quantization, tokenizer,
chat-template and metadata) before execution.
