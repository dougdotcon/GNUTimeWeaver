# Negative tests

The negative matrix covers missing/mismatched runtime, model, tokenizer,
manifest, cursor, sequence and corrupted/truncated objects. Real execution is
pending; mocks cannot promote formal status.
