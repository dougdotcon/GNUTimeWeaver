# G2 acceptance (future G2-A)

Workload `W0-E1` is frozen at 924 tokens, block size 128: seven complete blocks and a 28-token tail. Only complete, new blocks may be persisted.

Pass requires real `save_kv_layer` calls, proven logical/physical mapping, byte copies with unchanged dtype, valid checksums, complete per-layer manifests, event/payload join confidence `PROVEN`, read-back equality, zero load/external-match claims, bounded backpressure, fault-injection coverage, and clean shutdown. No restore, prefill avoidance, CoW, GPU or VRAM claim is part of G2.
