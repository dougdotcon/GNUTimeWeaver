# GNU TimeWeaver vLLM Connector v0.4.0 — G2

Status: `G2_PROTOCOL_PREPARATION_IN_PROGRESS` (no payload execution).

G2 saves real KV payloads only. Restore, external matching, CoW, GPU and distributed execution are out of scope. The v0.3.1 protocol is frozen.

Runtime is pinned to vLLM `v0.23.0`, commit `0fc695fc6d1d82e9a5ac6835ac8e4e1c83703665`, CPU, one engine, tensor/pipeline parallelism 1. The only future mode is `TIMEWEAVER_VLLM_MODE=kv_payload_save_only`; load methods remain fail-closed and external-token counters remain zero.

Before G2-A, G2-L0 must observe the actual registered cache tensors and save hooks. No dimension, K/V ordering, block slice, stride, or block-hash/physical-ID relationship may be assumed. Persistence requires `mapping_confidence=PROVEN`.

Payload objects use `timeweaver-vllm-kv-object/1`; manifests use `timeweaver-vllm-kv-block-manifest/1`. Objects are layer-scoped, checksummed, staged, fsynced, validated, atomically renamed, and only then included in a complete manifest. See the companion object-format, layout-observation, acceptance and failure-model documents.

Preparation result: `G2_PROTOCOL_READY_NO_PAYLOAD_EXECUTION` only after all preparation artifacts and fingerprints are frozen.
